---
const currentYear = new Date().getFullYear();
---

<footer class="footer">
  <div class="footer-content">
    <span class="footer-logo">
      <span class="bracket">&lt;</span><span class="df">df</span><span class="bracket">/&gt;</span>
    </span>
    <span class="copyright">© {currentYear} Davide Fella</span>
  </div>
</footer>

<style>
  .footer {
    border-top: 1px solid var(--border-subtle);
    padding: var(--space-xl) 0;
    margin-top: auto;
  }

  .footer-content {
    max-width: var(--max-width);
    margin: 0 auto;
    padding: 0 var(--space-xl);
    display: flex;
    justify-content: center;
    align-items: center;
    gap: var(--space-lg);
  }

  .footer-logo {
    font-family: var(--font-mono);
    font-size: 1rem;
    font-weight: 600;
  }

  .bracket { color: var(--blue-bright); }
  .df { color: var(--gold); }

  .copyright {
    color: var(--text-muted);
    font-size: 0.875rem;
  }
</style>
